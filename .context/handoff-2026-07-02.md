# Handoff — 2026-07-02 (resume point)

Branch: `fable_react`. Typecheck: `cd mobile && npx tsc --noEmit` → clean at this point.
Verify combined view: `node mobile/scripts/shot-combined.mjs bqWji3dnLE4LeXqFQDl5` (needs `npx expo start --web --port 8082` running; post-login navigations must use `domcontentloaded`, NOT `networkidle` — Firestore socket never idles).

## Uncommitted changes (COMMIT FIRST on resume)
User committed before these; the following are done + tsc-clean + Playwright-verified but NOT committed:
- `mobile/app/master.tsx` — moved `FamilyLegend` ABOVE `MasterScreen` (fixed `ReferenceError: FamilyLegend is not defined`, a Fast-Refresh forward-ref crash).
- `mobile/src/shared/treeLayout.ts` — `layoutLayered` ordering changed to hierarchical (children sorted by parent position) so siblings stay contiguous; removed barycenter reorder sweeps (they interleaved families → sib-bars shot across canvas = the "broken tree"). Also earlier: added `layoutLayered` + `relativeGenerations` (signed BFS: parent −1, child +1, spouse/sibling = same row — fuses two families at a marriage).
- `mobile/scripts/shot-combined.mjs` — combined-view screenshot tool.

Suggested commit msg: `fix(combined): join bridged families into one tree + fix FamilyLegend crash`

## IN PROGRESS — Task 14: combined-view chrome (was mid-rewrite of master.tsx)
User asks for the combined (master) view: (a) distinct accent + background OFF purple, (b) a real toolbar with Settings and other buttons. User confirmed "separate master view is fine."

Planned `master.tsx` rewrite (NOT yet applied):
- Combined accent `CV = c.amber ?? c.accent` (distinct from the app's purple). Apply to SegTabs active tab, toolbar icons, Bridges/Manage buttons. Add a faint canvas background wash: an absolute-fill `View` `backgroundColor: CV, opacity: 0.03` behind the canvas (import `StyleSheet`).
- Toolbar buttons (desktop toolbar right side + a compact icon row in mobile header): **Search**, **Members**, **AI chat**, **Settings**, **ThemeToggle** (+ existing **Bridges**).
- State: `panel: null|'settings'|'chat'|'members'`, `searchOpen: boolean`.
- Reuse existing panels (signatures verified):
  - `SettingsPanel({ onClose })`
  - `ChatPanel({ members, relationships, onOpenMember, onClose })` — feed combined members/relationships; `onOpenMember` → `openProfile`.
  - `MembersPanel({ members, meId, onOpenProfile, onOpenFamilyInfo?, onClose })` — combined members; `onOpenFamilyInfo` → `openBridges`.
  - `ThemeToggle` + `Avatar` from `src/ui/primitives`.
- Desktop: render panel OR `DesktopProfile` inside the existing `DesktopDrawer` (`open={!!drawerId || !!panel}`). Mobile: panels in a full-screen `<Modal>`.
- Search overlay (Modal, shared): TextInput + filtered combined members (name includes query, show family via `colorOf`); pick → `setFocusId(m.id)` + close (+ `setDrawerId(m.id)` on desktop). This also satisfies Task 15 (C8) for the combined view; still add search to the main `app/tree.tsx` / DesktopWorkspace too.

## Remaining tasks (approved this session)
- **15 · C8 search** — jump-to-person search in combined (above) + main tree/desktop. Filter members, select → focus + open profile.
- **16 · C4 invite links + QR** — deep link `family-tree://join?code=` + web URL; QR in `FamilyInfoPanel`. Invite codes already exist in `firebase/families.ts`. Needs a QR lib (ask before adding dep) or render QR via SVG.
- **17 · C2 notifications** — `expo-notifications`: schedule birthday (member.birthDate) + anniversary (spouse `marriageDate`) reminders. Data + `EventsPanel` exist. Native firing needs a dev build; logic/web works. (Install prunes `react-native-worklets` — re-add `@0.8.1` after any npm install, see mobile/CLAUDE.md.)
- **18 · C5 GEDCOM** — parse + emit GEDCOM; wire into `ExportPanel`/`importData.ts` reusing `planMerge`/`commitMerge`.
- **19 · C7 activity log + D polish** — lightweight who-changed-what trail on shared trees; empty/error states (replace silent `console.warn`), loading skeletons, onboarding nudge.

## Decisions / constraints (locked)
- Goal = public launch. Combined view is a **separate** specialized view (ok per user).
- **B (hardening) is LAST and stays OFF.** Do NOT touch Google sign-in — it already works. B4 budget/email-verify + B6 EAS = build-but-inactive later.
- **B2** (Cloudflare Gemini proxy + rotate key) and **B3** (App Check reCAPTCHA) still need USER's consoles — not startable here. Firestore rules ALREADY deployed (masters live).
- **C3** (photos→Firebase Storage) DEFERRED — needs paid Firebase/Blaze plan.
- **C6** (story enrichment) LAST.
- Combined edits route to origin tree via namespaced `treeId:localId` (`firebase/masters.ts` `splitId`). Relation delete disabled in combined desktop drawer (synthetic union edge ids).

## Live test data (in user's account, uid IqGt2SbHHZZfqkHS3qnZeGXxpYR2)
- Master "Mehta + Rao" = `bqWji3dnLE4LeXqFQDl5` (Mehta uid-tree + Rao), bridged on Priya (`same`).
- Rao Family tree = `QdsGVr9wWDiRmMQel3YX`. Re-seed via `node mobile/scripts/seedSecondFamily.mjs --yes` (dup-guarded).
- User's dev server sometimes runs on port **8081** (screenshots), shot script expects **8082**.

## Key files (this session's work)
- Master Families: `mobile/src/firebase/masters.ts` (CRUD + `suggestBridges` + `buildMasterData` union + `treeSig`), `useMasterTree.ts`, `app/master.tsx`, `app/combine.tsx`.
- Kinship rewrite: `mobile/src/shared/kinship.ts` (`descentKinship` LCA calc), `relationshipLogic.ts`, `relTerms.ts`.
- Layout join: `mobile/src/shared/treeLayout.ts` (`layoutLayered`, `relativeGenerations`).
- Viz coloring/props: `TreeView.tsx`, `RadialView.tsx`, `NetworkView.tsx` (`colorOf`, `layered`), `src/shared/networkLayout.ts`.
- Memory: `~/.claude/projects/.../memory/master-families-c1.md` has the running log.
